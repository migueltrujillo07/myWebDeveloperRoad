// Booleans can be only: "True" or "False"
let myTrueBool = true
let myFalseBool = false

// If statement
// The if statement will run code un between its blocksif the condition is true
if (condition){

}
// Logical operators are often used to form the condition

let currentMoney;
let laptopPrice;

if (currentMOney >= laptopPrice){
  console.log("Getting a new laptop!");
}

// if.. else statement 
// The "else" statement will run the code in between its blocks when condition
// is false. It's optional with an "if" statement

let currentMoney2;
let laptopPrice2;

if (currentMoney2 >= laptopPrice2){
  console.log("Getting a new laptop")
}
else{
  //condition is false. Code in this block will run
  console.log("Can't afford a new laptop, yet");
}

// Negation operator 
 if (!condition){
   // Run if condition is false
 } else {
  // Runs if condition is True
 }

// Ternary Expresions
// if ... else  isn't the only way to express decision logic.
// We can also use something called ternary operator. The sintax
// Looks like this:

let variable = condition ? < return this if true > : <return this if false>

let   firtsNumber = 20;
let secondNumber = 10;
let biggetsNumber = firtsNumber > secondNumber ? firstNumber: secondNumber;




  
